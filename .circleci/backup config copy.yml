version: 2.1

jobs:
  test:
    docker:
      - image: cimg/node:20.11
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: npm install
      - run:
          name: Run tests with coverage
          command: npm test -- --coverage

workflows:
  test-workflow:
    jobs:
      - test
